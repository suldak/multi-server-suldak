server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/suldaksuldak?useSSL=false&allowPublicKeyRetrieval=true&useUnicode=true&serverTimezone=Asia/Seoul
    username: root
    password: 1234
  main:
    allow-bean-definition-overriding: 'true'
#  profiles:
#    include: oauth
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: c14c29936ab91bf8d5ff8ccf7ab0c06f
            #          redirect-uri: "http://localhost:8080/login/oauth2/code/{registrationId}"
            redirect-uri: "http://localhost:8080/api/v1/oauth/kakao"
            client-authentication-method: POST
            authorization-grant-type: authorization_code
            scope: account_email,age_range,birthday,profile_nickname #동의 항목
            client-name: Kakao
          naver:
#            redirect-uri: '{baseUrl}/{action}/oauth2/code/{registrationId}'
            redirect-uri: 'http://localhost:8080/api/v1/oauth/naver'
            authorization-grant-type: authorization_code
            client-secret: dGE2n3Mrej
            scope: name,email,nickname,gender,birtday
            client-id: FaJvqOp2iYsiESXYnkeT
            client-name: Naver
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id
          naver:
            user-name-attribute: response
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            user-info-uri: https://openapi.naver.com/v1/nid/me
            token-uri: https://nid.naver.com/oauth2.0/token

jwt:
  key: "SULDAKSULDAK_KEY"